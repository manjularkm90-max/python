def flatten(lst):
    result = []
    for item in lst:
        if isinstance(item, list):
            result.extend(flatten(item))  # recursion
        else:
            result.append(item)
    return result

# Test
print(flatten([1, [2, [3, 4], 5], 6]))
