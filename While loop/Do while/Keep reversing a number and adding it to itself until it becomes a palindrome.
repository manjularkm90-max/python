def is_palindrome(n):
    return str(n) == str(n)[::-1]

num = int(input("Enter number: "))

while True:  # do-while simulation
    if is_palindrome(num):
        print("Palindrome reached:", num)
        break
    rev = int(str(num)[::-1])
    num = num + rev
