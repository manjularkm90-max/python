def subsets(nums):
    n = len(nums)
    
    for i in range(1 << n):  # 2^n
        subset = []
        
        for j in range(n):
            if i & (1 << j):
                subset.append(nums[j])
        
        print(subset)


subsets([1,2,3])
